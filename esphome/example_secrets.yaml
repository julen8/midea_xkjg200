# ESPHome 密钥配置文件
# 请根据实际情况修改以下配置

# WiFi 配置
wifi_ssid: "YOUR_WIFI_SSID"
wifi_password: "YOUR_WIFI_PASSWORD"

# Home Assistant API 加密密钥 (32字节 base64 编码)
# 可以使用以下命令生成: openssl rand -base64 32
api_encryption_key: "YOUR_API_ENCRYPTION_KEY"

# OTA 更新密码
ota_key: "YOUR_OTA_PASSWORD"
